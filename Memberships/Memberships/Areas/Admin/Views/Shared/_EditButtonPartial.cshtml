@model int

<a type="button" class="btn btn-primary btn-sm"
   href="@Url.Action("Edit", new { ItemId = Model })">
    <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
    <span>Edit</span>
</a>
